{
  "name": "sql-intro",
  "version": "1.0.0",
  "description": "- How to create a database  - `createdb albums-browser` (this is a bash command)",
  "main": "index.js",
  "scripts": {
    "db:delete": "dropdb albums-browser",
    "db:create": "createdb albums-browser",
    "db:schema": "psql -f schema.sql albums-browser",
    "db:seed": "psql -f seed.sql albums-browser",
    "db:reset": "npm run db:delete && npm run db:create && npm run db:schema && npm run db:seed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eva-montgomery/sql-demo.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/eva-montgomery/sql-demo/issues"
  },
  "homepage": "https://github.com/eva-montgomery/sql-demo#readme"
}
